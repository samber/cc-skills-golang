# Disabled: ClawHub publishing is not currently active.

name: Publish skills to ClawHub (disabled)

on:
  workflow_dispatch: {}

jobs:
  publish:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled"
